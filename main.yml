---
- hosts: localhost
  connection: local

  roles:
  - bluetooth
  - cleanup
  - display
  - fan
  - grub
  - kernel
  - sound
  - wifi
  
  tasks:
  - name: reboot gpd pocket
    command: /sbin/reboot
    async: 0
    poll: 0
    ignore_errors: true