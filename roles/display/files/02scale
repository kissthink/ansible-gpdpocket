# -*- sh -*-
# Xsession.d script to set scaling factor for display
#
# This file is sourced by Xsession(5), not executed.

if [ -f /usr/bin/gsettings ]; then
  gsettings set org.gnome.desktop.interface scaling-factor 1
fi